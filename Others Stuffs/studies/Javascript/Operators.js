let x = 10, y = 10;

let exponetial = x ** y;
let concat = x + y + "70"; //concat numbers type and strings

if(x === y)
{ console.log('Verify if are equals in type and value!'); }

if(x !== y)
{ console.log('Verify if not equal in type or value!'); }